{
  "_comment": "Template only — do not copy directly. The bootstrap prompt generates the correct config for your machine with absolute paths.",
  "mcpServers": {
    "fold": {
      "command": "/path/to/node",
      "args": [
        "/absolute/path/to/fold-ai/fold-mcp/build/index.js"
      ]
    }
  }
}
